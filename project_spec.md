# 블로그 글 자동화 서비스 — 프로젝트 스펙

> 내 블로그 말투를 학습해서 정보+사진만 입력하면 복붙 가능한 완성본을 만들어주는 웹 서비스.
> 기존 Claude Project 워크플로를 독립 서비스로 확장.

---

## 1. 서비스 개요

**핵심 가치**: "주제만 넣으면 대량 생성"하는 범용 AI 글쓰기 툴이 아니라, **내 말투를 학습한 개인화된 대필 비서**.

**타겟**: 네이버 블로그로 맛집/카페/체험 후기를 꾸준히 쓰는 개인 블로거, 소상공인 마케팅 담당자.

**차별점**
- 스타일 샘플 3개로 말투 학습 (범용 AI 글쓰기 툴 대비)
- 사진 개수 무제한 (Claude 앱 UI의 10장 제한 회피)
- 실제 배치된 완성본 미리보기 제공 (텍스트만 던지는 경쟁 서비스 대비)
- 네이버 api 를 연결하여 별도의 사진 배치 할 필요 없이 포스팅까지 완료

**경쟁 서비스 참고**: 에이블로, 비젠소프트, 알파블로그 등 이미 "AI+네이버 블로그" 시장 존재 → 수요는 검증됨, 포지셔닝 차별화 필요.

---

## 2. 가격 모델

**프리미엄(Freemium) 구조로 시작**, 아직 미정 현재 개발은 테스트만 할 것임

| 플랜 | 내용 |
|---|---|
| 무료 | 월 2~3회 생성, 완성본 미리보기까지 전체 기능 제공 (제한 없음) |
| 구독 (1차) | 월 구독 9,900~19,900원 — 생성 횟수 대폭 확대 또는 무제한, 사진 무제한 |
| 구독 (2차, 검증 후) | 네이버 자동등록/예약발행 포함 — 상위 플랜으로 분리 |

- 1회성 무료 체험이 아니라 **매달 소량 무료 + 구독 시 확장** 구조 (재방문 유도)
- 무료 단계에서도 결과물 품질은 100% 완전하게 (신뢰 형성이 우선)

---

## 3. 기능 범위

### Phase 1 (MVP) — 지금 개발 대상
1. 스타일 샘플 3개 업로드 → 말투 학습 (프로젝트 지식 방식과 동일한 원리)
2. 정보 입력 폼: 위치, 영업시간, 메뉴, 가격, 특이사항 등 (기존 지침 구조 그대로)
3. 사진 업로드 무제한 (Supabase Storage)
4. Claude Vision으로 사진 자동 분류 (외관/내부/메뉴판/음식) → **사용자가 분류 결과 확인/수정 가능한 단계 필수** (오분류 대응)
5. **완성본 미리보기 화면**: 텍스트 마커가 아니라 실제 사진이 배치된 모습을 HTML로 렌더링 → 이게 유료 가치의 최소 기준선
6. 복붙용 순수 텍스트 출력 (마크다운 서식 없이, 네이버 스마트에디터 대응)

### Phase 2 — 검증 후 추가
7. 네이버 블로그 오픈API(`writePost.json`) 프로토타입 테스트
   - ⚠️ 확인된 사실: 로그인 오픈API로 공식 글쓰기 API는 존재하나, **별도 이미지 업로드 엔드포인트가 API 목록에 없음** → 사진 첨부 자동화는 별도 검증 필요
   - 소규모 테스트 계정으로 먼저 사진 포함 발행이 안정적으로 되는지 확인 → 되면 상위 플랜 기능으로 편입
8. 예약발행 스케줄링
9. 결제/구독 연동 (토스페이먼츠 정기결제)

---

## 4. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트+백엔드 | Next.js (App Router) | 야장맵/럭키또와 동일 패턴, 재사용 가능 |
| DB/스토리지/인증 | Supabase (Postgres + Storage + Auth) | 사진 무제한 업로드, 기존 프로젝트와 동일 스택 |
| AI | Anthropic Claude API (Vision 지원 모델) | 말투 학습 텍스트 생성 + 사진 자동 분류 |
| 결제 | 토스페이먼츠 정기결제 API | 국내 구독형 SaaS 표준 |
| 배포 | Vercel | Next.js 최적화, 설정 최소화 |
| 개발 방식 | Claude Code | 기존 야장맵 개발 방식과 동일 |

---

## 5. 데이터베이스 스키마 (초안)

```sql
-- 사용자별 스타일 학습 샘플
style_samples
- id
- user_id
- content (text)
- created_at

-- 생성 요청/결과
posts
- id
- user_id
- location, hours, menu_items (jsonb)
- generated_text
- status  -- 'draft' | 'preview_ready' | 'published'
- created_at

-- 업로드 사진 + 분류 결과
post_images
- id
- post_id
- storage_url
- category      -- 'exterior' | 'interior' | 'menu' | 'food' | null
- category_confirmed  -- boolean (사용자 확인 여부)
- sort_order

-- 구독 상태
subscriptions
- id
- user_id
- plan          -- 'free' | 'pro'
- generation_count_this_month
- renewed_at
```

---

## 6. 개발 순서 제안

1. Next.js + Supabase 프로젝트 셋업 (기존 야장맵 보일러플레이트 재활용 가능)
2. 정보 입력 폼 + 사진 업로드 UI
3. Claude API 연동: 사진 분류 → 사용자 확인 단계 → 텍스트 생성
4. 완성본 미리보기 렌더링 화면 (여기까지가 "결제할 맛" 나는 최소 단위)
5. 무료/구독 횟수 제한 로직 (구독 결제 연동 전엔 임시로 수동 플래그 처리 가능)
6. 배포 후 소규모로 직접 써보며 말투 학습 품질 검증
7. (병행) 네이버 오픈API 사진 첨부 프로토타입 별도 테스트

---

## 7. 열린 질문 / 검증 필요 사항

- [ ] 네이버 `writePost.json`으로 사진 포함 발행이 실제로 안정적인지 (별도 테스트 계정 필요)
- [ ] Claude Vision 사진 분류 정확도 — 실제 사진 샘플로 테스트 필요
- [ ] 토스페이먼츠 정기결제 심사/입점 조건 확인